export enum LoadingStatus {
    initial = 'initial',
    loading = 'loading',
    success = 'success',
    error = 'error',
    updated = 'updated',
}
export enum TableOrder {
    ASC = 'ASC',
    DESC = 'DESC',
}
